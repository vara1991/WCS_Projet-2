{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Item {{ item.title }}</h1>
    <p>{{ item.id }}. {{ item.title }}</p>
    <a href="/Admin/edit/{{ item.id }}">Edit</a>
    <a href="/Admin/delete/{{item.id}}">Supprimer</a>
    <a href="/Admin/index">Back</a>
{% endblock %}
